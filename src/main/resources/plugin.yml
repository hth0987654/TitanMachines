name: ${project.artifactId}
version: ${project.version}
main: ${project.groupId}.${project.artifactId}
api-version: ${minecraft.api-version}
authors: [ freethemice ]
description: ${project.description}
website: ${project.url}
softdepend:
  - SensibleToolbox
  - Slimefun
  - WildStacker
depend:
  - TitanBoxLibs
commands:
  titanmachines:
    description: TitanMachines
    usage: /<command>
  tm:
    description: TitanMachines
    usage: /<command>
